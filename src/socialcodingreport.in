#!@PYTHON@

import sys
import signal
import os
import gi

# Add the prefix to path if needed, though installed packages should be in path.
# For local run/dev, path adjustment might be needed.
# @pkgdatadir@ will be substituted by meson.

sys.path.insert(1, '@pkgdatadir@')

from socialcodingreport.main import main

if __name__ == '__main__':
    # Handle SIGINT for Ctrl+C
    signal.signal(signal.SIGINT, signal.SIG_DFL)
    sys.exit(main())
