using Gtk 4.0;
using Adw 1;

template $MainWindow: Adw.ApplicationWindow {
  title: "Social Coding Report";
  default-width: 900;
  default-height: 600;

  content: Adw.ViewStack view_stack {
    Adw.ViewStackPage {
      name: "report";
      title: "Report";

      child: $ReportPage report_page {};
    }

    Adw.ViewStackPage {
      name: "preferences";
      title: "Preferences";

      child: $PreferencesPage preferences_page {};
    }
  };
}
