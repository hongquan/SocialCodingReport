---
name: Python GObject
description: Python GObject skill
---

## ListStore

The `Gtk.ListStore` is iterable, so do not use `for i in range(store.get_n_items())` to iterate over it. Instead, use `for item in store`.
